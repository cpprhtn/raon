# 보안·재현성 정책 (POLICY)

raon은 **취약점 발견 연구 도구**다. 그 성격상 권한·윤리·재현성 규약을 명시한다.

---

## 1. 권한 있는 사용만 (Authorized Use Only)

raon은 다음에만 사용한다:
- **본인이 소유**하거나 **명시적 테스트 권한**이 있는 소프트웨어
- 공개 벤치마크(Magma, Juliet/SARD) 및 **이미 공개된** OSS-Fuzz 과거 버그
- CTF·보안 연구·교육 등 승인된 맥락

허가 없이 제3자 시스템을 대상으로 삼는 것을 **금지**한다. 대량 타겟팅·서비스 거부·탐지
회피 목적의 사용을 금지한다.

## 2. 책임 있는 공개 (Responsible Disclosure)

raon이 **실제 OSS/제품**에서 미공개 취약점을 발견하면:
1. 공개 저장소·이슈·소셜에 **PoC/상세를 즉시 게시하지 않는다.**
2. 해당 프로젝트의 보안 연락처(SECURITY.md / security@ / 벤더 채널)로 **비공개 보고**한다.
3. 합리적 수정 기간(관례상 90일)을 부여한 뒤 조율된 공개를 따른다.
4. Magma 같은 벤치마크의 **재주입된 CVE**는 이미 공개된 것이므로 이 조항의 대상이 아니다.

## 3. 재현성 규약 (Reproducibility)

실험은 반드시 재현 가능해야 한다:
- **컨테이너 고정**: 툴체인(clang/llvm) 버전을 `docker/Dockerfile`로 고정.
- **LLM I/O 전량 로깅**: `raon.llm.JsonlLogger`가 프롬프트·응답·모델·usage·비용을 JSONL로 기록.
- **프롬프트 해시 캐싱**: `raon.llm.PromptCache`로 동일 프롬프트→동일 응답 재생
  (최신 모델은 temperature를 거부하므로 결정성은 캐싱으로 확보).
- **시드/버전 기록**: 코퍼스 시드 출처, 데이터셋 버전, 커밋 해시를 실험 로그에 남긴다.

## 4. 데이터셋 라이선스

- **Magma** (Apache-2.0), **Juliet/SARD** (공개 도메인/NIST) — 각 라이선스를 준수해 사용.
- 재배포 시 원 라이선스·출처를 표기한다.

## 5. LLM 안전

- raon이 호출하는 LLM은 **전략층**에만 관여한다(하네스 합성·트리아지·추론). 프롬프트에
  제3자 비밀·크리덴셜을 넣지 않는다.
- 합성된 하네스/코드는 **격리된 환경**(컨테이너)에서만 컴파일·실행한다.

---

이 정책에 어긋나는 사용으로 인한 결과에 대해 프로젝트와 기여자는 책임지지 않는다(LICENSE의
무보증 조항 참조).
